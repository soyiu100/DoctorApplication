server:
  port: 5000
  tomcat:
    use-relative-redirects: true

logging:
  level:
    org:
      springframework:
        security: INFO
        web:
          filter:
            CommonsRequestLoggingFilter: INFO